import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.deora.test.upsc',
  appName: 'Penni',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
}

export default config
